services:
  - type: web
    name: scroll-sentry-backend
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GOOGLE_CLIENT_ID
        value: "YOUR_WEB_CLIENT_ID.apps.googleusercontent.com"
